name: Sync from mruby

on:
  schedule:
    - cron: "23 17 * * *"
  workflow_dispatch:
    inputs:
      mruby_ref:
        description: "mruby/mruby ref to sync from"
        required: false
        default: master

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: sync-from-mruby
  cancel-in-progress: false

jobs:
  sync:
    if: github.repository == 'picoruby/mruby-bin-mrbc2'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout mruby-bin-mrbc2
        uses: actions/checkout@v6
        with:
          path: mruby-bin-mrbc2

      - name: Checkout mruby
        uses: actions/checkout@v6
        with:
          repository: mruby/mruby
          ref: ${{ inputs.mruby_ref || 'master' }}
          path: mruby

      - name: Sync mrbc gem
        id: sync
        working-directory: mruby-bin-mrbc2
        env:
          SOURCE_DIR: ../mruby/mrbgems/mruby-bin-mrbc
        run: |
          set -euo pipefail

          if [ ! -d "$SOURCE_DIR" ]; then
            echo "mruby-bin-mrbc was not found in mruby checkout" >&2
            exit 1
          fi

          MRUBY_SHA="$(git -C ../mruby rev-parse HEAD)"

          rsync -a --delete \
            --exclude='.git/' \
            --exclude='.github/' \
            --exclude='.gitmodules' \
            "$SOURCE_DIR/" ./

          echo "mruby_sha=$MRUBY_SHA" >> "$GITHUB_OUTPUT"
          echo "mruby_short_sha=${MRUBY_SHA:0:12}" >> "$GITHUB_OUTPUT"

      - name: Create sync pull request
        working-directory: mruby-bin-mrbc2
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          MRUBY_SHA: ${{ steps.sync.outputs.mruby_sha }}
          MRUBY_SHORT_SHA: ${{ steps.sync.outputs.mruby_short_sha }}
        run: |
          set -euo pipefail

          if git diff --quiet --exit-code && git diff --cached --quiet --exit-code; then
            echo "No changes to sync."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          BASE_BRANCH="$(gh repo view --json defaultBranchRef --jq '.defaultBranchRef.name')"
          SYNC_BRANCH="sync/mruby-bin-mrbc-${MRUBY_SHORT_SHA}"

          git switch -C "$SYNC_BRANCH"
          git add -A
          git commit -m "Sync mruby-bin-mrbc from mruby ${MRUBY_SHORT_SHA}"
          git push --force-with-lease origin "$SYNC_BRANCH"

          {
            echo "Synchronize this repository from mruby/mruby@${MRUBY_SHA}."
            echo
            echo "Source path:"
            echo "\`mrbgems/mruby-bin-mrbc\`"
            echo
            echo "This repository is a downstream mirror for users that need to pin mrbc as an independent mrbgem."
          } > pr-body.md

          EXISTING_PR="$(gh pr list --head "$SYNC_BRANCH" --state open --json number --jq '.[0].number // empty')"
          if [ -n "$EXISTING_PR" ]; then
            gh pr edit "$EXISTING_PR" \
              --title "Sync mruby-bin-mrbc from mruby ${MRUBY_SHORT_SHA}" \
              --body-file pr-body.md \
              --base "$BASE_BRANCH"
          else
            gh pr create \
              --title "Sync mruby-bin-mrbc from mruby ${MRUBY_SHORT_SHA}" \
              --body-file pr-body.md \
              --base "$BASE_BRANCH" \
              --head "$SYNC_BRANCH"
          fi
